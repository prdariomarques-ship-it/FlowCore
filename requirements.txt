# FlowCore — All dependencies (Core + API)
# For installing everything, use this file.
# For core-only installation, use requirements-core.txt
# For API-only (after core), use requirements-api.txt

-r requirements-api.txt
