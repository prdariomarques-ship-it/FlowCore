# FlowCore — Python dependencies
# Compatible with Termux (Android) and Python 3.11+
#
# IMPORTANT (Termux users):
#   pydantic-core requires Rust compilation, which fails on Termux.
#   install.sh handles this automatically by using the Termux User
#   Repository (TUR) for pre-built wheels.
#
#   If installing manually:
#     pip install pydantic-core --extra-index-url https://termux-user-repository.github.io/pypi/
#     pip install -r requirements.txt

# Core
fastapi>=0.104.0,<1.0.0
uvicorn>=0.24.0,<1.0.0
pydantic>=2.5.0,<3.0.0
pydantic-settings>=2.1.0,<3.0.0

# HTTP client (used by agents for external API calls)
httpx>=0.25.0,<1.0.0

# Scheduling (for daemon/scheduler)
apscheduler>=3.10.0,<4.0.0

# Logging
loguru>=0.7.0,<1.0.0

# Utilities
python-dotenv>=1.0.0,<2.0.0
pyyaml>=6.0,<7.0

# Database (lightweight SQLite, no external DB server needed)
aiosqlite>=0.19.0,<1.0.0
sqlalchemy>=2.0.0,<3.0.0

# Note: SQLAlchemy uses pure-Python engine by default.
# C extensions are optional and not required.
